# CINeMA runtime dependencies — direct imports only (pip resolves the transitive
# deps: CUDA libs, SimpleITK, etc.). Install: pip install -r requirements.txt
#
# Lower bounds only. `torch` is left portable — the default PyPI wheel works on
# CPU and (on Linux) bundles CUDA. For a specific CUDA build, install torch from
# https://pytorch.org/get-started/ (e.g. --index-url .../whl/cu128) instead.

torch>=2.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
nibabel>=5.0
PyYAML>=6.0
torchio>=0.19            # data augmentation (cinema/data.py)
matplotlib>=3.6          # sampling-distribution histograms (cinema/data.py)
scikit-learn>=1.1        # latent-analysis backends: NCA / kNN / PLS (cinema/evaluator.py)

--- Optional: ANTs-registered PSNR/SSIM/Dice for `cinema evaluate` ---
    (cinema/evaluator_ants.py; imported lazily, skipped if absent)
antspyx==0.4.2
scikit-image>=0.21

# --- Optional: experiment logging during `cinema train` ---
# wandb>=0.16

# --- Dev / tests (pytest) ---
# pytest
